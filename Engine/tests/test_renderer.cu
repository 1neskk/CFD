#include <gtest/gtest.h>
#include "Graphics/renderer.h"

TEST(RendererTest, Initialization) {
    GTEST_SKIP() << "Renderer tests require a running Application/Vulkan context. Skipping for now.";
}

TEST(RendererTest, Resize) {
    GTEST_SKIP() << "Renderer tests require a running Application/Vulkan context. Skipping for now.";
}
